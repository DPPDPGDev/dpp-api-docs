type: array
description: ''
minItems: 1
uniqueItems: true
examples: []
items:
  type: object
  properties:
    field_id:
      type: string
      minLength: 1
      description: Required if no `label` provided.
    value:
      type: string
    label:
      type: string
      minLength: 1
      description: Required if no `field_id` provided.
  required:
    - field_id
    - label
